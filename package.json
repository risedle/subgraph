{
  "name": "risedle-dashboard",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dimasriat/risedle-dashboard",
    "create-local": "graph create --node http://localhost:8020/ dimasriat/risedle-dashboard",
    "remove-local": "graph remove --node http://localhost:8020/ dimasriat/risedle-dashboard",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dimasriat/risedle-dashboard"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.3",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.0.0",
    "@typescript-eslint/parser": "^2.0.0",
    "eslint": "^6.2.2",
    "eslint-config-prettier": "^6.1.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.2"
  }
}